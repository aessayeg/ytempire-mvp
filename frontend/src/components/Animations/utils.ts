/**
 * Animation Utilities
 * Helper functions and constants for animations
 */

import {  fadeInUp, fadeInScale, slideInLeft, slideInRight, staggerContainer, staggerItem  } from './variants';

export const AnimationUtils = {
  fadeInUp,
  fadeInScale,
  slideInLeft,
  slideInRight,
  staggerContainer,
  staggerItem
};